@model IEnumerable<AdminSite.Models.AssociateLink>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Link.URL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserRole.RoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Link.URL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserRole.RoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssociateLinkID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssociateLinkID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssociateLinkID })
        </td>
    </tr>
}

</table>
