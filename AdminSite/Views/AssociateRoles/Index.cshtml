@model IEnumerable<AdminSite.Models.AssociateRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Account.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserRole.RoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Account.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserRole.RoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssociateRoleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssociateRoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssociateRoleID })
        </td>
    </tr>
}

</table>
